name: Publish picoemu (npm + GitHub Packages)

on:
  workflow_dispatch:
    inputs:
      branch:
        description: "Branch to publish from"
        required: true
        default: "main"
      version:
        description: "Version (semver, e.g. 0.50.0)"
        required: true
        default: "0.50.0"
      description:
        description: "Package description"
        required: true
        default: "Bramble RP2040/RP2350 (M0+/M33/RV32) browser emulator (WASM)"

permissions:
  contents: read
  packages: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.branch }}

      - name: Validate version (semver)
        run: |
          if ! echo "${{ inputs.version }}" | grep -Eq '^[0-9]+\.[0-9]+\.[0-9]+(-[0-9A-Za-z.-]+)?$'; then
            echo "Invalid semver: ${{ inputs.version }}"; exit 1
          fi

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org

      - name: Generate web/package.json from inputs
        env:
          PKG_VERSION: ${{ inputs.version }}
          PKG_DESC: ${{ inputs.description }}
        run: |
          node -e '
            const fs = require("fs");
            const pkg = {
              name: "picoemu",
              version: process.env.PKG_VERSION,
              description: process.env.PKG_DESC,
              type: "module",
              main: "bramble.wasm.js",
              license: "MIT",
              repository: { type: "git", url: "https://github.com/danish9661/picoemu.git" },
              files: ["bramble.wasm.js", "bramble.wasm.threads.js", "bramble.wasm.wasm", "bramble.wasm.threads.wasm", "bramble_worker.js", "index.html", "*.uf2", "examples/*.uf2"],
            };
            fs.writeFileSync("web/package.json", JSON.stringify(pkg, null, 2) + "\n");
            console.log(fs.readFileSync("web/package.json", "utf8"));
          '
          test -f web/bramble.wasm.js -a -f web/bramble.wasm.wasm -a -f web/index.html

      - name: Vendor MIT license into package (upstream attribution)
        run: cp LICENSE web/LICENSE

      - name: Smoke test (native build + WASM boot)
        run: |
          cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
          cmake --build build -j
          ctest --test-dir build --output-on-failure
          node test-wasm.js

      - name: Pack (dry-run contents check)
        working-directory: web
        run: npm pack --dry-run

      - name: Publish to npmjs (picoemu)
        working-directory: web
        run: |
          if [ -z "$NODE_AUTH_TOKEN" ]; then
            echo "::error::NPM_TOKEN secret is missing or empty. Add it under repo Settings -> Secrets and variables -> Actions -> New repository secret (npmjs automation token)."
            exit 1
          fi
          printf '//registry.npmjs.org/:_authToken=%s\n' "$NODE_AUTH_TOKEN" > ~/.npmrc
          npm publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Rename to repo scope for GitHub Packages
        working-directory: web
        run: |
          node -e '
            const fs = require("fs");
            const p = JSON.parse(fs.readFileSync("package.json", "utf8"));
            p.name = "@danish9661/picoemu";
            fs.writeFileSync("package.json", JSON.stringify(p, null, 2) + "\n");
          '

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          registry-url: https://npm.pkg.github.com
          scope: "@danish9661"

      - name: Publish to GitHub Packages (@danish9661/picoemu)
        working-directory: web
        run: |
          printf '@danish9661:registry=https://npm.pkg.github.com\n//npm.pkg.github.com/:_authToken=%s\n' "$NODE_AUTH_TOKEN" > ~/.npmrc
          npm publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
